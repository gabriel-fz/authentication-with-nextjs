import React from 'react';
import HeadDefault from 'next/head';

const Head: React.FC = () => {
  return (
    <HeadDefault>
      <title>Autenticação com NextJs</title>
    </HeadDefault>
  );
};

export default Head;
